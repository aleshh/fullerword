export interface Entry {
  // id: number,
  text: string,
  urlText?: string,
  definition: string,
  source?: string,
  tags: string[],
  star?: boolean;
  sampleData?: boolean;
  // synonyms: string[],
  // strength: number,
  // share: string,
  // dateAdded: Date
}